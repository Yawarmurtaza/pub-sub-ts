import { Lru } from "../LRU";

describe("LRU Test Suite", () => {
    test("Add should add key value pair", () => { 
        // Arrange
        const lru = new Lru();
    });

    
    test("Get should return existing data using key", () => { 
        // Arrange
        const lru = new Lru();
    });


});